-- Those SQL queries work on both Redshift and Postgres
-- get current user
select user;
select current_user;

-- get current database
select current_database();

-- get version
select version();

-- get datetime
select now();

-- get current date
select current_date;

